# Related News Block

Auto-created on news article pages, inserted below the hero (h1 + image). Placeholder for future implementation.

## Usage

The block is automatically built by `buildRelatedNewsBlock()` in `scripts/scripts.js` when:
- The page has `body.news` (Template: news)
- The hero section exists

## Structure

Empty placeholder — to be implemented later.
